using System;
using Godot;

public class Inventory
{
    // Thing that can hold items in slots.
    // Items of same type are put together in same slot
    // Has a maximum amount of slots
    // Has a maximum amount of items per slot
    // Has a maximum total items mass
    // Has a maximum total items volume
    // Does not do any displaying or whatever, use extending/wrapping classes to do that
}